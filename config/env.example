# 모두의핏 웹사이트 환경 설정
# 실제 배포 시 .env 파일로 복사하여 사용

# 기본 설정
NODE_ENV=production
PORT=80

# 도메인 설정
DOMAIN=all4fit.co.kr
WWW_DOMAIN=www.all4fit.co.kr
PROD_DOMAIN=prod.all4fit.co.kr
DEV_DOMAIN=dev.all4fit.co.kr

# SSL 설정
SSL_EMAIL=admin@all4fit.co.kr
SSL_CERT_PATH=/etc/letsencrypt/live/all4fit.co.kr/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/all4fit.co.kr/privkey.pem

# 웹사이트 설정
WEB_ROOT=/var/www/all4fit
BACKUP_DIR=/var/backups/all4fit
LOG_DIR=/var/log/all4fit

# 보안 설정
SECRET_KEY=your-secret-key-here
JWT_SECRET=your-jwt-secret-here

# 데이터베이스 설정 (필요한 경우)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=all4fit
DB_USER=all4fit_user
DB_PASSWORD=your-db-password

# 이메일 설정 (필요한 경우)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# 모니터링 설정
MONITORING_ENABLED=true
LOG_LEVEL=info
