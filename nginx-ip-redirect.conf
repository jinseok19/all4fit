# IP에서 도메인으로 리디렉션
server {
    listen 80;
    server_name 3.38.85.149;  # 실제 EC2 IP
    
    # IP에서 도메인으로 리디렉션
    return 301 http://all4fit.co.kr$request_uri;
}

# HTTP에서 HTTPS로 리디렉션 (ACME 챌린지 예외 포함)
server {
    listen 80;
    server_name all4fit.co.kr www.all4fit.co.kr prod.all4fit.co.kr dev.all4fit.co.kr;
    
    # Let's Encrypt HTTP-01 챌린지 경로 (여기는 리다이렉트 금지)
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type "text/plain";
        try_files $uri =404;
    }

    # 그 외에는 HTTPS로 리다이렉트
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS 메인 사이트
server {
    listen 443 ssl http2;
    server_name all4fit.co.kr www.all4fit.co.kr;
    
    # SSL 인증서 경로
    ssl_certificate /etc/letsencrypt/live/all4fit.co.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/all4fit.co.kr/privkey.pem;
    
    # SSL 보안 설정
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # 웹사이트 루트 디렉토리
    root /var/www/all4fit;
    index index.html;
    
    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;
    
    # 정적 파일 캐싱
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # HTML 파일 캐싱
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public";
    }
    
    # 메인 페이지
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 로그 설정
    access_log /var/log/nginx/all4fit_access.log;
    error_log /var/log/nginx/all4fit_error.log;
}

# 프로덕션 서브도메인
server {
    listen 443 ssl http2;
    server_name prod.all4fit.co.kr;
    
    # SSL 인증서 경로
    ssl_certificate /etc/letsencrypt/live/all4fit.co.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/all4fit.co.kr/privkey.pem;
    
    # SSL 보안 설정
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # 웹사이트 루트 디렉토리
    root /var/www/all4fit;
    index index.html;
    
    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;
    
    # 정적 파일 캐싱
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # HTML 파일 캐싱
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public";
    }
    
    # 메인 페이지
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 로그 설정
    access_log /var/log/nginx/all4fit_prod_access.log;
    error_log /var/log/nginx/all4fit_prod_error.log;
}

# 개발 서브도메인
server {
    listen 443 ssl http2;
    server_name dev.all4fit.co.kr;
    
    # SSL 인증서 경로
    ssl_certificate /etc/letsencrypt/live/all4fit.co.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/all4fit.co.kr/privkey.pem;
    
    # SSL 보안 설정
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # 웹사이트 루트 디렉토리 (개발용)
    root /var/www/all4fit-dev;
    index index.html;
    
    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;
    
    # 정적 파일 캐싱 (개발용으로 짧게)
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1h;
        add_header Cache-Control "public";
        access_log off;
    }
    
    # HTML 파일 캐싱 (개발용으로 짧게)
    location ~* \.html$ {
        expires 5m;
        add_header Cache-Control "no-cache";
    }
    
    # 메인 페이지
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 로그 설정
    access_log /var/log/nginx/all4fit_dev_access.log;
    error_log /var/log/nginx/all4fit_dev_error.log;
}
